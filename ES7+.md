### ES7
```
[].inCludes(xxx)    判断是否存在
2 ** 10     Math.pow(2,10)    幂运算
```
### ES8
```
async    await
对象扩展
1.Object.keys();
2.Object.values();
3.Object.entries();
4.Object.getOwnPropertyDescripors();
```
### ES9
```
对象 rest 参数
对象扩展运算符
正则扩展
1.命名捕获分组
2.反向断言    /(?<=么)\d+/    正向断言    /(?<=么)\d+/  
3.dotAll    /./s
```
### ES10
```
对象扩展
1.Object.fromEntries()    二维数组 转 对象
字符串扩展方法
1.trimStart    清除开始空白
2.trimEnd    清除结尾空白
3.trim        清除两边空白
数组扩展
1.flat(1)    数组扁平
2.flatMap()    map + flat
Symbol扩展
1.Symbol.prototype.description        获取描述字符串

```
### ES11
```
私有属性
1.class    #age
Promise.allSettles
返回状态：成功 resolved    
返回参数：所有promise对象
字符串扩展
1.String.prototype.mathAll()
可选链操作符
1.?.
动态    import
1.import(./xxx.js).then(module => {})
数据类型：BigInt
1.大整数：521n
2.函数：BigInt(123)
全局this
1.globalThis

```